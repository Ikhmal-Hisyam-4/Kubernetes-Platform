```bash
terraform init       # Download providers (run once)
terraform plan       # Preview changes
terraform apply      # Provision VMs
terraform output     # Show IPs and IDs
terraform destroy    # Delete all VMs
```
